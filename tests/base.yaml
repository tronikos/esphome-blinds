---
# Minimal device scaffolding shared by the config-validation tests.
# Not intended for flashing; see the README for a real config.
esphome:
  name: test-blinds
  friendly_name: Test Blinds
esp8266:
  board: d1_mini
  restore_from_flash: true
logger:
wifi:
  ssid: "test-ssid"
  password: "test-password"
